import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
	container: {
		flexGrow: 1,
		justifyContent: 'center',
		marginHorizontal: 30
	},
	input: { marginVertical: 10 },
	actionButton: { marginTop: 25 },
	cameraContainer: { flexGrow: 1, justifyContent: 'center' },
	useWholeSpace: { height: '100%', width: '100%' }
});
